$('table#tasks tbody').append('<%= escape_javascript render(@task) %>');
<% if (@task.current_page < @task.num_pages) %> 
  $('.pagination').replaceWith('<%= escape_javascript paginate(@task, :theme => "twitter-bootstrap") %>');
<% else %>
  $('.pagination').remove();
<% end %>
<% sleep 1 %>